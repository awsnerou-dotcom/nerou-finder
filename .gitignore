node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.claude/
emails.json
assets/uploads/

# Postgres is the sole source of truth (see server-db.ts) - data.json is at most a local
# migration input, never a live store, and must never be committed (it can contain real
# user data including password hashes when generated locally).
data.json

# Leftover SQLite artifact from before the app migrated to a Postgres-only schema.
prisma/dev.db
